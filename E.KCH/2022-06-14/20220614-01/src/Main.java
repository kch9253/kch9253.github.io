public class Main {
	public static void main(String[] args) {
		// Object = 최상단 클래스 끝판왕
		// 자식 1,2,3 이 있는경우
		// Object o = new 자식1(); 이렇게 사용할경우
		// 자식1 안에 있는 값들을 아무렇게나 불러와서 사용할수있다.
		// 예를 들어 자식1의 toStirng 값이 있다면 (o.toString()) 을 통해 자식1의 toString 값을 불러와서 사용할수있다.
		// 사용할수있는 이유는 new 자식1() 을 통해 선언을 해줬기때문에
		
	}
}
